# Spring Boot приложение для управления пользовательскими запросами с CI/CD пайплайнами

## 🌐 Production URL

**https://university-practice-crud-app.onrender.com/api/v1/requests**
### Бесплатная версия уходит в режим ожидания, поэтому открытие может занять около минуты
---

## 🔌 REST API

### 👤 Users

| Метод   | Эндпоинт              | Описание                |
|---------|-----------------------|-------------------------|
| GET     | `/api/users/{id}`     | Пользователь по ID      |
| POST    | `/api/users`          | Создать пользователя    |

**Пример POST `/api/users`**
```json
{
  "name": "Иван",
  "email": "ivan@example.com"
}
```

---

### 📝 Requests

| Метод   | Эндпоинт                 | Описание             |
|---------|--------------------------|----------------------|
| GET     | `/api/requests`          | Все запросы          |
| GET     | `/api/requests/{id}`     | Запрос по ID         |
| POST    | `/api/requests`          | Создать запрос       |
| PUT     | `/api/requests/{id}`     | Обновить запрос      |
| DELETE  | `/api/requests/{id}`     | Удалить запрос       |

**Пример POST `/api/requests`**
```json
{
  "title": "Проблема с доступом",
  "description": "Не могу зайти в личный кабинет"
}
```
---


## ✅ CI/CD

- **CI:** GitHub Actions  
  При создании Pull Request в `main` запускаются тесты, checkstyle и сборка Docker-образа.
- **CD:** Автоматический деплой на Render при мерже PR в `main`
